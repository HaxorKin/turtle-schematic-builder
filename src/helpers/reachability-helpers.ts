export const isTurtleReachable = (reachability: number) => reachability >= 0;
export const isBlock = (reachability: number) => reachability === -2;
export const isEmpty = (reachability: number) => reachability >= -1;
